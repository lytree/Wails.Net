// WailsNET Desktop Runtime - 自动生成，请勿手动修改
(function() {
  // 后端通过此方法解析挂起的请求（保留兼容性，当前 HTTP 模式不需要）
  window._wailsResolve = function(id, result) {
    // HTTP 模式下响应通过 fetch 直接返回，此方法保留用于未来 postMessage 模式
  };
})();
